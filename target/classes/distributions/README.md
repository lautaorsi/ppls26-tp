# Distributions

The distributions themselves are pretty standard and have no unexpected behaviours, the only currently supported distributions are Normal (Gaussian) and Bernoulli.

Note that the distributions performs the sampling but the message itself is called by the [Contoller](../controllers)