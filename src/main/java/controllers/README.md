# Controllers

The controllers handle all stochastic operations, they handle a runInference() method which executes the specific algorithm depending on the controller type. 

They are also the responsible for sending the sampling/observation messages to the distributions.



